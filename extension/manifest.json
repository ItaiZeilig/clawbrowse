{
  "manifest_version": 3,
  "name": "JevBridge",
  "version": "0.1.0",
  "description": "Lets Claude Code drive your real, logged-in Chrome via a local bridge. jev-style element-table perception; the agent is the policy (no external model, no keys).",
  "minimum_chrome_version": "120",
  "permissions": ["debugger", "tabs", "storage", "alarms"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js", "type": "module" },
  "options_page": "options.html",
  "action": { "default_title": "JevBridge" }
}
